// M1 stub. M3 replaces this with two real states: a pairing-code input (before the
// extension has a token) and a post-submission quality-rating prompt (after).
export function Popup() {
  return (
    <div style={{ padding: 16 }}>
      <strong>CodeReps</strong>
      <p style={{ fontSize: 13, color: '#555' }}>Pairing and rating UI arrive in M3.</p>
    </div>
  );
}
